# Using an example of list comprehension. Which is combining the for loop and new elements into one line.
# Just another way of doing the square_numbers.py

# No colon is necessary in this for loop

squares = [value**2 for value in range(1, 11)]
print(squares)
